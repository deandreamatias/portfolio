name: portfolio
description: deandreamatias
version: 2.1.0
code: 16

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  google_fonts: ^6.2.1
  intl: ^0.19.0
  unicons: ^3.0.0
  url_launcher: ^6.3.0

dev_dependencies: 
  flutter_lints: ^4.0.0

flutter:
  generate: true